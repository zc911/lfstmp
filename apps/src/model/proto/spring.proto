syntax = "proto3";

package dg.model;
import "common.proto";

// ## Business Intelligence APIs
service SpringService {
    // ### Index APIs
    rpc IndexVehicle (VehicleObj) returns (NullMessage) {
    }
    rpc BingoVehicle (VehicleObj) returns (NullMessage) {
    }

}



