AUX_SOURCE_DIRECTORY(. SRC_LIST)
AUX_SOURCE_DIRECTORY(config SRC_CONFIG)
AUX_SOURCE_DIRECTORY(model SRC_MODEL)
AUX_SOURCE_DIRECTORY(model/proto SRC_PROTO)
AUX_SOURCE_DIRECTORY(service SRC_SERVICE)

LIST(APPEND SRC_LIST ${SRC_MODEL} ${SRC_CONFIG} ${SRC_PROTO} ${SRC_SERVICE})

ADD_EXECUTABLE(witness ${SRC_LIST})

TARGET_LINK_LIBRARIES(witness ${LIBS})

INSTALL(TARGETS witness RUNTIME DESTINATION ${PROJECT_SOURCE_DIR}/bin/${CMAKE_BUILD_TYPE})
